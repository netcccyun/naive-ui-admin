import componentSetting from '@/settings/componentSetting';

const { table } = componentSetting;

const { apiSetting, defaultPageSize, pageSizes, pageSlot, displayOrder } = table;

export const DEFAULTPAGESIZE = defaultPageSize;

export const APISETTING = apiSetting;

export const PAGESIZES = pageSizes;

export const PAGESLOT = pageSlot;

export const DISPLAYORDER = displayOrder;
